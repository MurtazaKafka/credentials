const express = require('express');
const router = express.Router();

// Placeholder for student routes
router.get('/', (req, res) => {
  res.json({ message: 'Student routes not implemented yet' });
});

module.exports = router;