const express = require('express');
const router = express.Router();

// Placeholder for high school routes
router.get('/', (req, res) => {
  res.json({ message: 'High school routes not implemented yet' });
});

module.exports = router;